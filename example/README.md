# pixel_border_example

A sample project to demonstrate the usage of PixelBorder package.

To run the sample application first install the pub dependencies with

```
flutter pub get
```

and then run it with

```
flutter run
```
